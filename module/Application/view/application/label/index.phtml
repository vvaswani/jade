
            <div class="panel panel-default">
                <div class="panel-heading clearfix">
                    <span class="panel-title"> <?php echo $this->translate('label.current-labels'); ?></span>
                    <div class="pull-right">
                        <?php if ($this->authorize()->isAuthorized($this->identity(), 'label', 'save')): ?>
                        <a role="button" class="btn btn-primary btn-sm" href="<?php echo $this->url('labels', array('action' => 'save')); ?>">
                            <span class="glyphicon glyphicon-plus"></span> <?php echo $this->translate('common.create'); ?>
                        </a>
                        <?php endif; ?>
                    </div>                    
                </div>
                <div class="table-responsive">
                    <table class="table table-striped">
                        <tbody>
                            <?php foreach ($this->labels as $l): ?>
                            <tr>
                                <td><span class="label label-default" style="background-color: <?php echo $this->escapeHtml($l->getColour()); ?>"><?php echo $this->escapeHtml($l->getName()); ?></span></td>
                                <td>
                                    <?php if ($this->authorize()->isAuthorized($this->identity(), 'label', 'save', $l)): ?>
                                    <a role="button" class="btn btn-primary btn-sm" href="<?php echo $this->url('labels', array('action' => 'save', 'id' => $this->escapeHtml($l->getId()))); ?>">
                                        <span class="glyphicon glyphicon-edit"></span> <?php echo $this->translate('common.edit'); ?>
                                    </a>
                                    <?php endif; ?>
                                </td>
                                <td>
                                    <?php if ($this->authorize()->isAuthorized($this->identity(), 'label', 'delete', $l)): ?>
                                    <a role="button" class="btn btn-danger btn-sm" data-remote="false" data-toggle="modal" data-target="#confirm-modal" href="<?php echo $this->url('labels', array('action' => 'delete', 'id' => $this->escapeHtml($l->getId()))); ?>">
                                        <span class="glyphicon glyphicon-remove"></span> <?php echo $this->translate('common.delete'); ?>
                                    </a>
                                    <?php endif; ?>
                                </td>
                            </tr>            
                            <?php endforeach; ?>
                        </tbody>
                    </table>
                </div>
            </div>
